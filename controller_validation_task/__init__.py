"""Controller validation task.

A lightweight PsychoPy task that validates an fMRI/MEG/EEG-compatible game
controller: a controller diagram is shown, a cue indicates whether the next
press should be short or long, one button lights up, and the participant
presses (and releases) it with the matching timing.

Ported from ``task_stimuli/src/tasks/gamepad.py`` into a standalone,
config-driven repo. See README.md.
"""

__version__ = "0.1.0"
